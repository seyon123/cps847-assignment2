language: python

python:
  - 3.6
  - 3.7
  - 3.8

before_install:
  - python --version
  - pip install pip
  - pip install pytest
  - pip install pytest-cov
  - pip install codecov
  
script: pytest --cov=./ # run tests

after_success:
  - codecov # submit coverage
